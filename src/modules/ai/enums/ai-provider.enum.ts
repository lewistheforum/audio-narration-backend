/**
 * AI Provider Enum
 *
 * Defines the supported AI service providers for the Bonix Backend.
 * Each provider has its own service implementation and API integration.
 */
export enum AiProvider {
  GEMINI = 'GEMINI',
  CHATGPT = 'CHATGPT',
}
